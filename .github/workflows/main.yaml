on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to run action
    steps:
      - name: Run action
        id: action
        uses: kcl-lang/actions@main
        run: kcl -V
